import { createSlice } from "@reduxjs/toolkit";

const CreateMeetSlice = createSlice({
    name: 'CreateMeet',
    initialState: {
        value: 0
    },
    reducers: {

    }
})
export const {} = CreateMeetSlice.actions

export default CreateMeetSlice.reducer