import { createSlice } from "@reduxjs/toolkit";

const AuthFormSlice = createSlice({
    name: 'Auth',
    initialState: {
        value: 0
    },
    reducers: {

    }
})
export const {} = AuthFormSlice.actions

export default AuthFormSlice.reducer