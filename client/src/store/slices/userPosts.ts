import { createSlice } from "@reduxjs/toolkit";

const userPosts= createSlice({
    name: 'CreateMeet',
    initialState: {
        value: 0
    },
    reducers: {

    }
})
export const {} = userPosts.actions

export default userPosts.reducer