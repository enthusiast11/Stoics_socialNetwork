import { createSlice } from "@reduxjs/toolkit";

const postList= createSlice({
    name: 'PostList',
    initialState: {
        value: 0
    },
    reducers: {

    }
})
export const {} = postList.actions

export default postList.reducer