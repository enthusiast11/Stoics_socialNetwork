import { createSlice } from "@reduxjs/toolkit";

const createMeet= createSlice({
    name: 'createMeet',
    initialState: {
        value: 0
    },
    reducers: {

    }
})
export const {} = createMeet.actions

export default createMeet.reducer